rm -f ~/.*.zwc(N.)
find \
    ${SSO}(N/) \
    -name '*.zwc' \
    -print0 \
    | xargs -0 rm

_zcompile-custom ~/.zshrc ~/.zshenv ~/.zlogin
_zcompile-custom ${SSO}/*.{zshrc,zshenv,zlogin,ztheme}(N^/)
_zcompile-custom ${SSO}/autoload/*(N^/)
_zcompile-custom ${SSO/lib/*(N^/)