update-dotfiles() {
  CUR_DIR="$PWD"
  cd "$HOME/.dotfiles" &> /dev/null || exit
  for i in $(find . -name '.git' | cut -c 3-); do
    cd "$i" &> /dev/null || exit
    cd .. &> /dev/null
    git pull --quiet
  done
  cd "$CUR_DIR" &> /dev/null || exit
  unset CUR_DIR i
echo "Done"
}